#include <iostream>
using namespace std;

int main(void)
{
	int t, r;
	cin >> t;
	string* ans = new string[t];
	char* temp;
	for (int i = 0; i < t; ++i)
	{
		cin >> r >> ans[i];
		temp = new char[ans[i].length() * r + 1];
		for (int j = 0; j < r; ++j)
		{
			ans[i]
		}
	}

	
	return 0;
}